describe("Others Auth", () => {
    beforeEach(() => cy.visit("/outros/uploaddearquivos"));

    it("Others Auth", () => {
        //Aqui nao é possível inspecionar um elemento, neste momento o JS Prompt é apresentado
    });
});